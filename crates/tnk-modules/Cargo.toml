[package]
name = "tnk-modules"
description = "tambanokano module system: the module database + import flattening (protecting/extending/including, summation, renaming) over the tnk-frontend pipeline"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
tnk-core = { path = "../tnk-core" }
tnk-frontend = { path = "../tnk-frontend" }
