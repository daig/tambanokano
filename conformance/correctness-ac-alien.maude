*** AC matching with "alien" (non-ground, non-variable) subterms.
***
*** A pattern lhs with a non-ground non-variable subterm directly under an AC operator — e.g. `s M` under
*** `+` in textbook commutative Peano addition. Each alien matches recursively against a subject
*** element; for `reduce` the greedy matcher binds the alien to the canonically-FIRST (smallest) matching
*** element (ACU_GreedyMatcher: findFirstPotentialMatch + the sorted-arg scan), determining the rewrite
*** count deterministically.

fmod PEANO-AC is
  sort Nat .
  op z : -> Nat [ctor] .
  op s_ : Nat -> Nat [ctor] .
  op _+_ : Nat Nat -> Nat [assoc comm] .
  vars M N : Nat .
  eq z + N = N .
  eq s M + N = s (M + N) .
endfm
red s z + s s z .          *** s s s z, 2 rewrites (alien s M -> the smaller s z)
red s s z + s s s z .      *** s s s s s z, 3 rewrites
red z + s z + s s z .      *** s s s z, 3 rewrites
red s z + s z + s z .      *** s s s z, 4 rewrites
red s s s z + s s s s z .  *** s s s s s s s z, 4 rewrites

fmod PEANO-AC-MUL is
  sort Nat .
  op z : -> Nat [ctor] .
  op s_ : Nat -> Nat [ctor] .
  op _+_ : Nat Nat -> Nat [assoc comm] .
  op _*_ : Nat Nat -> Nat [assoc comm] .
  vars M N : Nat .
  eq z + N = N .
  eq s M + N = s (M + N) .
  eq z * N = z .
  eq s M * N = N + (M * N) .
endfm
red s s z * s s s z .      *** 2 * 3 = 6
red s s s z * s s s z .    *** 3 * 3 = 9
