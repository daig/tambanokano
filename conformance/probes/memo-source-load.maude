*** Source-file `[memo]` attribute emits the unsupported-attribute warning and keeps the module usable.
fmod MEMO-FILE is
  sort N .
  op z : -> N [ctor] .
  op f : N -> N [memo] .
  eq f(z) = z .
endfm
