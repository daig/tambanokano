*** C4c-self-import — a module importing itself; oracle marks it unusable (fable-audit.md §3.6)
fmod M is
  protecting M .
  sort S .
  ops a b : -> S .
  op f : S -> S .
  var X : S .
  eq f(X) = b .
endfm
reduce in M : f(a) .
