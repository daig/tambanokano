*** C4f-print-attr-check — malformed [print] attr (unbound var); Maude drops the statement
fmod M is
  sort S .
  ops a b : -> S .
  op f : S -> S .
  var X : S .
  eq f(X) = b [print "got " Z] .
endfm
reduce in M : f(a) .
