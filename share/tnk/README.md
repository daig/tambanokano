# tnk-authored Maude facades (MIT)

Maude source that belongs to tambanokano, not to the stock Maude distribution.

| File | Provides |
|------|----------|
| `variant-satisfiability.maude` | `VAR-SAT` / `VAR-SAT-TOOL` facade over native `tnk-core::variant_sat` |

Licensed under the repository-root MIT License (`../../LICENSE`).

Put this directory on `$MAUDE_LIB` alongside `share/maude-gpl` and your external
Maude prelude directory.
